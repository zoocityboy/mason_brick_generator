import '';

typedef {{name.pascalCase()}}DetailEntities = List<{{name.pascalCase()}}DetailEntity>;

class {{name.pascalCase()}}DetailEntity extends {{name.pascalCase()}}Entity {
  const {{name.pascalCase()}}DetailEntity({
    required super.id,
    required this.name,
  });
  final String name;
  @override
  List<Object?> get props => [id, name];

  @override
  {{name.pascalCase()}}DetailEntity copyWith({
    int? id,
    String? name,
  }) {
    return {{name.pascalCase()}}DetailEntity(
      id: id ?? this.id,
      name: name ?? this.name,
    );
  }
}
